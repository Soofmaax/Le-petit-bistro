# Quick Start

## Frontend (SPA)

```
npm install
npm run dev
```

## Backend (optionnel)

```
npm run server:start
```

Endpoints:
- `GET /health`, `GET /ready`
- `POST /v1/auth/login` (démo)
- `POST /v1/reservations` (protégé)

SmarterLogicWeb — https://smarterlogicweb.com