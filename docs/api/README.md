# API Documentation

Cette section couvrira:

- Contrats (OpenAPI v1)
- DTOs (Zod schemas)
- Endpoints backend (quand présents)

## Contrats

- Préparer `openapi.yaml` pour la version 1.
- Générer des types côté frontend via `openapi-typescript`.

## Endpoints (baseline)

- `POST /v1/reservations` – crée une réservation
- `POST /v1/auth/login` – authentification démo
- `GET /v1/csrf` – token CSRF (cookies)

SmarterLogicWeb — https://smarterlogicweb.com