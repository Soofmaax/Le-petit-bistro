# Environment variables example
# Copy to .env and adjust values for local development.

# Optional: override the analytics domain used by Plausible.
# If unset, window.location.hostname will be used.
VITE_PLAUSIBLE_DOMAIN=le-petit-bistro.example

# Optional: show CookieConsent banner during tests (default: hidden in test)
# Set to "true" to render the banner in Vitest/UI tests.
VITE_ENABLE_COOKIE_BANNER_IN_TEST=false

# Reserved for future features:
# VITE_API_BASE_URL=https://api.example.com
# VITE_FEATURE_FLAGS=reservation:true,analytics:true

# Backend (when present):
# CORS_ALLOWLIST=https://le-petit-bistro.netlify.app,https://localhost:5173
# JWT_SECRET=change-me
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=100